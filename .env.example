# Base URL of the FastAPI RAG backend (no trailing slash).
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production example (set this in the hosting provider, do not commit secrets):
# NEXT_PUBLIC_API_URL=https://<render-backend>.onrender.com
